TypeTransformationBuilder<Person, ObjectNode> builder = transformationBuilderFactory.create();
builder.setSourceType(Person.class);
builder.setTargetTypeBuilder(dynamicEntityTypeSubrepository.createBuilder("PetLover"));
builder.transform().from("name").to("name");
builder.transform().from("insurane.price").to("price");
builder.transform().from("pets").to("petCount").converter(SizeConverter.class);


EntityTypeTransformation<Person, ObjectNode> personLoverTransformation =
	(EntityTypeTransformation<EntityA, DynamicEntity>) builder.buildTypeTransformation();


